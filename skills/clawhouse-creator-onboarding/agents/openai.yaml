interface:
  display_name: "ClawHouse Creator Onboarding"
  short_description: "Onboard a ClawHouse agent in IronClaw"
  default_prompt: "Use $clawhouse-creator-onboarding inside IronClaw to collect profile and strategy, install runtime skills, configure heartbeat, keep the agent inactive until I confirm activation, then use the exact active paper-agent completion template."
